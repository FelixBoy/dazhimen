<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dazhimen.bg.bean.PlaySort">
    <select id="listAllIndexPlay" resultType="dazhimen.bg.bean.IndexPlayBean">
        select a.pid,a.pname,getcodetxt('producttype',a.type) type
          from product a
        where a.status != '3' and a.isdel = '0' and a.indexplay = '1'
        <!--未来要 重新梳理一下字段，并拼接上 新闻模块-->
    </select>

    <select id="getAddIndexPlayData" resultType="dazhimen.bg.bean.IndexPlayBean">
          select a.pid,a.pname,getcodetxt('producttype',a.type) type
          from product a
        where a.status != '3' and a.isdel = '0' and a.indexplay = '0'
        <!--未来要 重新梳理一下字段，并拼接上 新闻模块-->
    </select>
    <update id="saveAddIndexPlay">
        update product
          set indexplay = '1'
        where pid = #{pid}
    </update>
    <update id="saveDeleteIndexPlay">
        update product
        set indexplay = '0'
        where pid = #{pid}
    </update>
    <select id="getAddSkillPackIndexSortData" resultType="dazhimen.bg.bean.indexsort.ProductIndexSortBean">
        select a.pid,a.pname,getcodetxt('producttype',a.type) type
          from product a
        where a.status != '3' and a.isdel = '0' and indexsort = '99' and type = '1'
    </select>
    <select id="getSkillPackNextSortPosition" resultType="java.lang.String">
        select count(b.pid)+1 from product b where b.type = '1' and b.status != '3' and b.indexsort != '99'
    </select>
    <update id="saveAddSkillPackIndexSort" parameterType="dazhimen.bg.bean.indexsort.UpdateSkillPackIndexSortBean">
        update product
        set indexsort = #{nextIndexSort}
        where pid = #{pid}
    </update>
    <select id="queryAllSkillPackIndexSort" resultType="dazhimen.bg.bean.indexsort.ProductIndexSortBean">
        select a.pid,a.pname,getcodetxt('producttype',a.type) type
          from product a
        where status != '3' and a.isdel = '0' and indexsort != '99' and type = '1'
        order by indexsort
    </select>
</mapper>