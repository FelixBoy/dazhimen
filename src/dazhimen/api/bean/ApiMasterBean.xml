<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dazhimen.api.bean.ApiMaster">
    <select id="getHomePageMasterInfo" resultType="dazhimen.api.bean.ApiMasterBean">
        select uid as masterid,name,identity,introduction,headerimg as headerimgurl
          from user
        where type = '1'
          and isdel = '0'
          order by indexsort,createdate desc
        limit #{getcount}
    </select>

    <select id="getMasterInfoById" resultType="dazhimen.api.bean.ApiMasterBean">
          select uid as masterid,name,identity,introduction,headerimg as headerimgurl
          from user
        where uid = #{masterid} and isdel = '0'
    </select>
</mapper>